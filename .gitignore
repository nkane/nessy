
# Claude Code local agent worktrees / state
.claude/worktrees/

# Built binaries (a bare `go build ./cmd/nessy` drops these in the repo
# root) + local release staging.
/nessy
/nessy-record
/dist/
/out/
/stage/
