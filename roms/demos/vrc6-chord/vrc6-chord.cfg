# VRC6 (mapper 24) — 16 KiB PRG = 2 × 8 KiB banks. Bank 0 lives at
# $8000-$9FFF when prgBank16 = 0 (unused here). Fixed bank (bank 1)
# lives at $E000-$FFFF + carries reset/NMI code + the vectors.

MEMORY {
    HEADER:  start=$00,    size=$0010,  fill=yes;
    BANK0:   start=$8000,  size=$2000,  fill=yes, fillval=$00;
    FIXED:   start=$E000,  size=$1FFA,  fill=yes, fillval=$00;
    VECTORS: start=$FFFA,  size=$0006;
    CHR:     start=$0000,  size=$2000,  fill=yes, fillval=$00;
    ZP:      start=$00,    size=$0100;
}

SEGMENTS {
    ZEROPAGE: load=ZP,      type=zp;
    HEADER:   load=HEADER;
    BANK0:    load=BANK0;
    FIXED:    load=FIXED;
    VECTORS:  load=VECTORS;
    CHARS:    load=CHR;
}
